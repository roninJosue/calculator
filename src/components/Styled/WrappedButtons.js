import styled from "styled-components";
import {calculatorWidth} from "../../styles/utils";

export const WrappedButtons = styled.div`
  display: flex;
  ${calculatorWidth};
  flex-wrap: wrap;
  justify-content: center;
`